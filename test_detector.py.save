from src.core.detectors import detect_editor
import subprocess

result = subprocess.run(['xdotool', 'getwindowname', '12582960'], capture_output=True, text=True)
title = result.stdout.strip()

print(f'Название окна: {title}')
print(f'Редактор: {detect_editor(title)}')


